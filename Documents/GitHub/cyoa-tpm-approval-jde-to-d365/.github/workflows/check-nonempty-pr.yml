name: 🧠 Check Pull Request Body

on:
  workflow_dispatch:
  pull_request:
    types: [opened, edited]

jobs:
  check-body:
    runs-on: ubuntu-latest
    steps:
      - name: Check PR body is not empty
        run: |
          if [ -z "${{ github.event.pull_request.body }}" ]; then
            echo "🚨 Pull request body is empty. Please provide a description."
            exit 1
          fi
